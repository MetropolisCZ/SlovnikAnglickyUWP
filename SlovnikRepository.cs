using System;
using System.Collections.Generic;
using System.IO;

namespace SlovnikAnglickyUWP
{
    public class SlovnikRepository
    {
		/*private List<Slovnik> _seznam;

		public List<Slovnik> SeznamSlov
		{
			get { return _seznam; }
			set { _seznam = value; }
		}
		


		private int _pocetSlov;

		public int PocetSlov
		{
			get { return _pocetSlov; }
			set { _pocetSlov = value; }
		}


        public void NaplnitSeznamSlov()
        {
            String txtSlovnikRadek;
            Slovnik slovnik = new Slovnik();

            Stream soubor = File.OpenRead("slovnik_data_utf8.txt");
			StreamReader cteniSouboru = new StreamReader(soubor);
			while( (txtSlovnikRadek = cteniSouboru.ReadLine() ) != null)
			{
				slovnik = new Slovnik();
				slovnik.AnglickeSlovo = txtSlovnikRadek;
				_seznam.Add(slovnik);

            }

        }


		public SlovnikRepository()
		{
			_seznam = new List<Slovnik>();
			NaplnitSeznamSlov();
			_pocetSlov = _seznam.Count;

        }*/


    }
}
